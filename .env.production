# Public, build-time analytics config for the deployed site.
# NEXT_PUBLIC_* values are inlined into the client bundle at `next build`, so they
# are public by design (the app key ships to every visitor). Committed on purpose,
# the same way afterhive's landing app carries its public build config.
NEXT_PUBLIC_COUNTLY_URL=https://countly.projects.manu-web.de
NEXT_PUBLIC_COUNTLY_APP_KEY=f5dea345fb038ddad0d7d824a6bdef1080879ebb
